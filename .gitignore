# 构建产物
build/
dist/

# 个人/本地文档(设计规格等,不公开)
docs/

# macOS
.DS_Store

# 采集样本(带情绪标签的私有音频)
samples/
*.mp3
*.wav
*.m4a
*.flac

# IDE / 工具
.idea/
.vscode/
.claude/

# Mimosa 安全扫描器的本地会话状态(hook 每次运行都会重写;曾因 git add -A
# 混入提交 —— 忽略之,防复发)
.mimosa/
