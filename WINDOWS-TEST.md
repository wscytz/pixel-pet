# PixelPet — Windows 真机测试清单

> Windows 是 PixelPet 的主阵地:**系统音频(WASAPI loopback)开箱即用**,任意桌面 app 的声音都能驱动宠物。
> 本清单给真机验证用。下载 `PixelPet-Windows.zip`,解压即测。

## 0. 安装

1. 解压 zip 到任意目录(如桌面)
2. 双击 `pixel-pet.exe` 运行(windeployqt 已打好 Qt 依赖,无需另装 Qt)
3. 宠物出现在屏幕角落,右键弹出菜单

> 首次运行若被 SmartScreen 拦(本地分发、未签名 exe):选「更多信息 → 仍要运行」。

## 1. 系统音频(WASAPI)—— 重点,王牌

1. 开任意桌面音乐客户端放歌
2. 宠物右键 → 设置 → 勾选「系统音频」(或在音源面板里开)
3. 宠物脸 / 音浪应随音乐动起来;切歌 / 切情绪,脸跟着变
4. **验证情绪映射**:
   - 欢快大调歌 → 欢快 / 治愈脸,色暖,跳动积极
   - 伤感慢歌 → 伤感 / 治愈脸,色冷,动作放慢
   - 电音 / dubstep → 愤怒 / 躁动脸,音浪激烈
5. **验证音停衰减**:暂停音乐 → 宠物约 1.5 秒内停下进 idle(**不再继续蹦**)

> WASAPI 抓「默认输出设备」的混音,任何 app 声音都经过这里。没反应时查:系统默认输出设备是不是放歌那个(蓝牙耳机 / 扬声器别选错);系统音量别静音。

## 2. 本地文件

1. 拖一首 `.mp3` / `.wav` 到宠物窗口,或命令行 `pixel-pet.exe <歌>`
2. 宠物随该文件反应(与系统音频共用同一分析链路,情绪映射一致)

## 3. 浏览器扩展(可选)

1. zip 里有 `PixelPet-浏览器扩展` 文件夹
2. 浏览器开 `edge://extensions`(或 `chrome://extensions`)→ 右上角「开发者模式」→「加载已解压的扩展程序」→ 选该文件夹
3. 打开任意网页播放站点 → 点扩展图标(或快捷键 **Ctrl+Shift+0**)开始抓音频
4. 宠物随网页音频反应

## 4. 观感走查

- 16 档脸清晰可辨?右键切透明度 / 锁定位置 / 音浪样式(柱状·镜像·波形)
- 拖拽移动 / 滚轮缩放 / 双击暂停 / 托盘恢复
- 不同窗口尺寸(150 迷你 ~ 520)脸仍可辨

## 5. 反馈记录

每项记 ✅ / ❌ + 现象(哪首歌、脸跳哪档、对不对)。问题反馈后再修。

## 已知边界(诚实)

- 系统音频抓**整个系统输出**,不按 app 过滤(多个 app 同时响会混在一起)
- 励志大调歌可能偏中性(调式阈值边界,持续优化中)
- 稠密混音的军鼓反拍(backbeat)检测是诊断列、不接宠物行为
